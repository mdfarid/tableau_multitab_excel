# Tableau JS Extension 


I have created an Excel Downloader function to extract data from tableau dashboard using Tableau JS APICall [e.g getWorkbook().activateSheetAsync ]
This function will enable users to download tableau report which includes multible dashboard sheets into a single file in xls format ,
